# Final QA・完成判定（PROMPT-0009）

- 実施日: 2026-04-15
- 対象: `game63/game1_upgrade/`
- 目的: Sprint 1〜3の主要導線と計測整合を最終確認し、機能追加スプリントを終了できるか判定する。

## 1. 主要導線チェック

- [x] 設定画面で 4項目（表示時間 / グリッド / モード / テンポ）を選ぶまで開始不可。
- [x] プレイ開始後、表示→非表示→再生の本質ループを維持。
- [x] 失敗時は `RetryScene` でひらがなヒントと再挑戦導線を表示。
- [x] 成功時は `ClearScene` で次ラウンド導線が維持。
- [x] 「さいしょから」でセッション終了イベント保存後に設定画面へ戻る。

## 2. Sprint 2 / 3 計測整合チェック

`game.js` のイベント保存ロジックとセッション保存項目を確認。

- [x] `order_retry_stage` が順序ミスごとに `stage_1` / `stage_2` で保存される。
- [x] 2連敗緩和時に `tempo_suggested` が保存される。
- [x] `reviewStats`（`total`, `success`）がセッション保存される。
- [x] `orderStats`（`attempted`, `completed`, `failed`, `recoveredAfterHint`）がセッション保存される。
- [x] 再来問題は `round_start.result = review` と `source_round_id` を保持できる。

## 3. 判定

- 判定: **完成（機能追加スプリント終了）**
- 理由:
  - PROMPT-0009 のDoD（主要導線・イベント整合・完成記録）を満たす。
  - 重大な欠陥は見当たらず、以後は保守・軽微調整モードで十分。

## 4. 次回運用

- 新規大型機能は追加しない。
- 受け付ける変更は以下に限定する。
  - 文言調整（5歳UIの読みやすさ向上）
  - バグ修正（クラッシュ / 進行不能）
  - 計測ログの欠損補修
