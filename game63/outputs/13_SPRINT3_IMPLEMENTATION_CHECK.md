# Sprint 3 実装・受け入れ条件チェック（PROMPT-0007）

- 実施日: 2026-04-15
- 対象: `game63/game1_upgrade/game.js`

## 1. 実装結果サマリ

- 設定画面にテンポ選択（`ゆっくり` / `ふつう`）を追加し、開始条件に組み込み。
- 表示フェーズで順番にパルスを出すリズムガイドを追加し、`rhythm_guide_played` をイベント保存。
- 色モードに順序タッチ判定を導入し、順番違いを `recall_fail.result = wrong_order` で記録。
- セッション保存に `tempo` と `rhythmStats.roundsWithGuide` を追加。

## 2. 受け入れ条件チェック

- [x] テンポ選択が必須入力として機能する。
- [x] `rhythm_guide_played` がラウンドイベントとして保存される。
- [x] 色モード順番ミスがログで追跡可能。
- [x] game1本体を変更せず game63 配下のみ更新。

## 3. 補足

- 順序判定により難易度が上がるため、次スプリントで段階的ヒント緩和を優先する。
- 本質ループ（表示→非表示→再生）は維持。
